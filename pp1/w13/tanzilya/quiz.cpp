#include <iostream>
#include <set>
#include <sstream>
using namespace std;
int amount (string s, string s2){
    for (int i = 0; i < s.size(); i++) {
        for (int j = 0; j < s2.size(); j++) {
            if (s[i] == s2[j]){
                
            }
        }
    }
    
    return count;
}
int main(){
    string sentence;
    getline (cin, sentence);    
    cout << amount(sentence);
}