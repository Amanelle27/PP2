#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <sstream>
// atoi() converts array of characters into integer

using namespace std;

int main(int argc, char *argv[]) {
    cout << argc <<endl;
    string op = argv[2];
    int a = atoi(argv[1]);
    int a = atoi(argv[3]);
    // i tam esli op == + to plus i td 
     return 0;
}